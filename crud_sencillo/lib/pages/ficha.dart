import 'package:flutter/material.dart';

class FichaScreen extends StatelessWidget {
   
  const FichaScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('FichaScreen'),
      ),
    );
  }
}